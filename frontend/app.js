// Shared Vite entry point. Page-specific inline behavior remains opt-in so
// the legacy Twig flows keep their existing load order during migration.
document.documentElement.dataset.v26Assets = 'ready';
